# Firebase App Distribution Configuration
# Edit these values to change default distribution settings

# Default tester emails (comma-separated)
firebase.appDistribution.testers="piyush.k@zebpay.com"

# Default tester groups (comma-separated)
firebase.appDistribution.groups="QA"

# App ID (optional - auto-detected from google-services.json if not set)
# firebase.appDistribution.appId=1:123456789:android:abcdef

# Service credentials file path (for local builds, not needed for CI/CD)
# firebase.appDistribution.serviceCredentialsFile=path/to/key.json
